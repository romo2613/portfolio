---
interface Props{
    class?: string
}

const { class: className } = Astro.props
---

<div class=`badge badge-primary badge-outline cursor-default transition ease-in-out hover:scale-110 ${className}`>
    <slot />
</div>
